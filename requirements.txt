# Serving dependencies for the Vercel Python function (api/index.py).
#
# Deliberately NOT the project's full dependency set. `pyproject.toml` is what a
# developer installs; this is the minimum the advisory API needs to answer a
# request, and the gap between them is the point:
#
#   xgboost + scikit-learn + scipy + pandas   358 MB   (training)
#   onnxruntime + numpy                        85 MB   (serving)
#
# Vercel's Python runtime allows 500 MB. The training stack fits only barely,
# and Linux wheels run larger than the Windows measurement above -- xgboost
# bundles libxgboost.so. Serving from ONNX removes that risk entirely rather
# than betting the submission deadline on a wheel size.
#
# Nothing here may drift from pyproject.toml in a way that changes an answer:
# the model is a fixed ONNX artifact, and the physics is pure Python.

fastapi>=0.115
pydantic>=2.9
numpy>=1.26
onnxruntime>=1.19

# Natural-language advisory layer. Two providers, because the guard that makes
# this layer safe is provider-agnostic and we would rather demonstrate that than
# assert it: Google is the default (a free tier a judge can reproduce without a
# card), Anthropic is the fallback for a rate-limited demo.
#
# Both are pure-Python HTTP clients, not frameworks, and neither moves the 85 MB
# serving figure above in a way a reader would notice. They are listed here
# rather than kept out of the serving image because the sentence the captain
# reads is part of the product, and because the layer is written to no-op
# without a key: no key, no import cost that matters, deterministic templates on
# the display.
google-genai>=1.0
anthropic>=0.75
