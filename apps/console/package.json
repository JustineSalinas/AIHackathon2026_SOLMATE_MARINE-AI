{
  "name": "@marine-ai/console",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Navigation console. Chart, pathfinder and voyage simulation; every recommendation comes from the Python API in apps/api.",
  "scripts": {
    "dev": "tsx server.ts",
    "build:wasm": "asc assembly/index.ts --outFile public/routing.wasm -O3",
    "build": "npm run build:wasm && vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "preview": "vite preview",
    "clean": "rm -rf dist server.js public/routing.wasm",
    "lint": "tsc --noEmit",
    "start": "node dist/server.cjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "driver.js": "^1.8.0",
    "express": "^4.21.2",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "assemblyscript": "^0.28.20",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2"
  },
  "license": "ISC"
}
